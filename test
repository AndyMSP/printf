#include "main.h"
#include <limits.h>

int main()
{
	int _len, len;

	_len = _printf("%d\n", INT_MIN);
	len = printf("%d\n", INT_MIN);

	printf("TEST END LINE: _len = %i, len = %i\n", _len, len);

	return (0);
}
